# Active pipeline dependencies. Supported interpreter: Python 3.11.x only.
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
xgboost==2.1.1
statsmodels==0.14.4
joblib==1.4.2
requests==2.32.3
python-dotenv==1.0.1
matplotlib==3.9.2
seaborn==0.13.2
jupyter==1.1.1
ipykernel==6.29.5
pytest==8.3.3

# Optional presentation/explanation tools; pinned so the same Python 3.11
# environment can install them without changing the project runtime.
streamlit==1.39.0
shap==0.46.0

# MLOps phase: local feature store (DuckDB) and model registry (MLflow).
# Both run with no server and no paid services, matching the assignment's
# 100% serverless/free-student-stack requirement (see Docs/mlops_architecture.md).
duckdb==1.1.3
mlflow==2.18.0

# Dashboard backend (FastAPI) used by the Streamlit app and demo API.
fastapi==0.115.6
uvicorn==0.32.1

# Deep learning: LSTM model for daily AQI forecasting.
# Only needed for training pipeline; dashboard skips gracefully if not installed.
tensorflow==2.17.0
