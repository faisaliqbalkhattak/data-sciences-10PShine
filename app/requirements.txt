# Dashboard dependencies for Streamlit Community Cloud.
# Strict subset of root requirements.txt: no mlflow, no fastapi, no
# jupyter, no pytest -- keeping the install fast and avoiding the pyarrow
# build failure (pyarrow needs cmake which Streamlit Cloud does not provide).
# TensorFlow is included so the dashboard can load a Keras/LSTM model if it
# becomes the champion during automated training.
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
joblib==1.4.2
requests==2.32.3
python-dotenv==1.0.1
matplotlib==3.9.2
streamlit==1.39.0
shap==0.46.0
duckdb==1.1.3
xgboost==2.1.1
tensorflow==2.17.0
