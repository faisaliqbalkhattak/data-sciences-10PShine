# Dashboard dependencies for Streamlit Community Cloud.
# Strict subset of root requirements.txt: no mlflow, no fastapi, no
# jupyter, no pytest -- keeping the install fast and avoiding the pyarrow
# build failure (pyarrow needs cmake which Streamlit Cloud does not provide).
# TensorFlow is NOT needed here -- it is only used for training the LSTM
# model in the CI pipeline. The dashboard loads the pre-trained Ridge model
# via joblib and never touches TensorFlow at runtime.
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
joblib==1.4.2
requests==2.32.3
python-dotenv==1.0.1
matplotlib==3.9.2
streamlit==1.39.0
shap==0.46.0
duckdb==1.1.3
xgboost==2.1.1
